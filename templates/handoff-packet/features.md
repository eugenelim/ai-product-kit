<!-- source: HANDOVERS-6 §"Folder contents" — `features.md` -->

# Features

This file satisfies `/audit-completeness` checklist item 11 ("Features are mapped to capabilities"). The `Parent capability id` column is the load-bearing traceability anchor: every Feature must name the Capability it serves, and those capability ids must match the rows in [`capabilities.md`](./capabilities.md) and the `parent_capability:` fields in [`requirements.yaml`](./requirements.yaml).

| Feature id | Feature name | Parent capability id |
| --- | --- | --- |
| <FEAT-NNN> | <feature name> | <CAP-NNN> |

> Duplicate the data row for each Feature in scope.
