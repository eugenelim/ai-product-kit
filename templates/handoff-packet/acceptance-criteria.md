<!-- source: HANDOVERS-6 §"Folder contents" — `acceptance-criteria.md ← per-requirement` -->

# Acceptance criteria

This file satisfies `/audit-completeness` checklist item 13 ("Acceptance criteria are defined per requirement"). Each H3 heading is the Requirement id (matching an entry id in [`requirements.yaml`](./requirements.yaml)); the bulleted list below it enumerates the criteria that prove the Requirement is met. The H3 heading text is the target of the `acceptance_criteria_ref:` field in `requirements.yaml`.

### <REQ-NNN>

- <criterion 1 — observable predicate>
- <criterion 2 — observable predicate>
- <criterion 3 — observable predicate>

> Duplicate the H3 block above for each Requirement in `requirements.yaml`.
