<!-- source: HANDOVERS-6 §"Folder contents" — `future-workflow.md ← how it'll work` -->

# Future workflow

This file pairs with [`current-workflow.md`](./current-workflow.md) and documents how the workflow will look after this packet ships. Together with `current-workflow.md` it shows engineering the delta they are building.

## Steps after shipping <!-- source: HANDOVERS-6 gloss "how it'll work" -->

1. <Step 1 — actor + action + system touched>
2. <Step 2 — actor + action + system touched>
3. <Step 3 — actor + action + system touched>
4. <Step 4 — actor + action + system touched>
5. <Step 5 — actor + action + system touched>

> Add or remove steps as the future workflow requires.

## What changes <!-- source: inferred (current-vs-future delta callout) -->

<One paragraph naming the delta from `current-workflow.md` — which steps are new, which are removed, which are unchanged.>
