<!-- source: HANDOVERS-6 §"Folder contents" — `current-workflow.md ← how it works today` -->

# Current workflow

This file satisfies `/audit-completeness` checklist item 6 ("Current workflow is documented"). A numbered list of the steps a target persona takes today, naming the actor, the action, and the system touched at each step.

## Steps today <!-- source: HANDOVERS-6 gloss "how it works today" -->

1. <Step 1 — actor + action + system touched>
2. <Step 2 — actor + action + system touched>
3. <Step 3 — actor + action + system touched>
4. <Step 4 — actor + action + system touched>
5. <Step 5 — actor + action + system touched>

> Add or remove steps as the actual workflow requires.
