# Fixture sentences — ears-lint

Eleven candidate requirement sentences with annotated expected classifications. Covers all seven verdict labels (Ubiquitous, Event-driven, State-driven, Optional-feature, Unwanted-behavior, Complex, Non-conformant) plus two edge cases surfaced by pre-EXECUTE adversarial review iter-1: a passive-voice sentence with `shall` but no named system actor, and an Unwanted-behavior sentence using `If` without the literal `then`. Manual-gesture verification (per `docs/specs/ears-lint/spec.md` Verification mode) compares each row's expected pattern against the in-session classification recorded under `docs/specs/ears-lint/notes/manual-verification-2026-05-23.md`.

| # | Sentence | Expected pattern | Rationale |
|---|---|---|---|
| 1 | The system shall log every API request. | ubiquitous | No leading EARS keyword; named system actor `The system`; consequence uses `shall`. Fall-through Ubiquitous classification. |
| 2 | When a user submits the checkout form, the system shall validate the card details. | event-driven | Leading `When` keyword with a discrete-event trigger (form submission); consequence uses `shall` and names the system. |
| 3 | While the user is authenticated, the system shall display the dashboard. | state-driven | Leading `While` keyword with a sustained state (authenticated session); consequence uses `shall` and names the system. |
| 4 | Where multi-factor authentication is enabled, the system shall require a second factor. | optional-feature | Leading `Where` keyword with an opt-in feature condition (MFA enabled); consequence uses `shall` and names the system. |
| 5 | If the payment gateway returns an error, then the system shall display a retry prompt. | unwanted-behavior | Leading `If` keyword with an exceptional precondition; explicit `then` keyword; consequence uses `shall` and names the system. Canonical Mavin form. |
| 6 | If the user's session expires, the system shall redirect to the login page. | unwanted-behavior | Leading `If` keyword with an exceptional precondition; comma-only form without `then` — kit-accepted per the framework's Unwanted-behavior H3. |
| 7 | When a user submits the checkout form, while the cart total exceeds $1000, the system shall require manager approval. | complex | Contains two leading EARS keywords (`When` and `While`) attached to distinct clauses — the Complex combination form. |
| 8 | Should the system support guest checkout? | non-conformant | Sentence is a question, not a requirement; ends with `?`. Belongs in the artifact's `## Open questions` section. Suggested rewrite: surface as an open question, not a requirement. |
| 9 | The system must validate the email field. | non-conformant | Modal verb is `must` instead of `shall`; the kit accepts `shall` only. Suggested rewrite: `The system shall validate the email field.` |
| 10 | The user should be able to log in via SSO. | non-conformant | Goal-style without named system actor (subject is `the user`, not the system); modal is `should`, not `shall`. Suggested rewrite: `When a user initiates SSO sign-in, the system shall delegate authentication to the configured identity provider.` |
| 11 | An email shall be sent to the user on checkout completion. | non-conformant | Passive voice with `shall` but no named system actor; the EARS templates require the named system as the grammatical subject of the consequence clause. Suggested rewrite: `When checkout completes, the system shall send a confirmation email to the user.` |
| 12 | The system shall validate the email and the system shall send a confirmation message. | non-conformant | Two Ubiquitous clauses glued with `and` — packs two requirements into one sentence without Complex keywords. Suggested rewrite: split into two separate requirements (`The system shall validate the email.` and `When email validation succeeds, the system shall send a confirmation message.`). |
