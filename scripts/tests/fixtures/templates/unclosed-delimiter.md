---
object_type: <Strategic Intent>
status: Draft
last_updated: <YYYY-MM-DD>

# <Artifact name>

Stub body. The opening `---` has no closing `---`, so the frontmatter parser
emits a `parse_errors` entry. `--check-template` must surface that error
rather than only blaming missing required fields.
